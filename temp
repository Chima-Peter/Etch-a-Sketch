const clickMeBtn = document.querySelector('#btn');
const body = document.querySelector('body');
const p = document.createElement('button');
const boxes = document.querySelectorAll('#div');

let numberOfBoxes = prompt('Enter number of boxes - no more than 100');
//x.style.borderRadius = '50%';
//x.style.padding = '20px';
body.appendChild(x);
clickMeBtn.addEventListener('click', () => {
    body.appendChild(p);
    p.textContent = "click me";
});
p.addEventListener('click', () => {
    body.removeChild(p);
})
boxes.forEach(box => {
    box.addEventListener("mouseover", () => {
        let colorValue = 0;
        box.style.backgroundColor = x.value;
    });
});
const randomColor = Math.floor(Math.random()*16777215).toString(16);
p.style.backgroundColor = "#" + randomColor;
console.log(p.style.backgroundColor);
p.style.opacity = 0;